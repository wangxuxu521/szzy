<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.springboot.mapper.TypeMapper">

    <resultMap id="TypeResultMap" type="com.example.springboot.entity.Type">
        <id property="typeId" column="type_id"/>
        <result property="typeName" column="type_name"/>
        <result property="description" column="description"/>
        <result property="parentId" column="parent_id"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="status" column="status"/>
    </resultMap>

    <select id="findById" parameterType="int" resultMap="TypeResultMap">
        SELECT * FROM type WHERE type_id = #{typeId}
    </select>

    <select id="findAll" resultMap="TypeResultMap">
        SELECT * FROM type ORDER BY sort_order, type_id
    </select>
    
    <select id="checkTypeNameExists" resultType="int">
        SELECT COUNT(*) FROM type WHERE type_name = #{typeName}
    </select>

    <select id="findChildrenByParentId" parameterType="int" resultMap="TypeResultMap">
        SELECT * FROM type WHERE parent_id = #{parentId} ORDER BY sort_order, type_id
    </select>
    
    <select id="countChildrenByParentId" parameterType="int" resultType="int">
        SELECT COUNT(*) FROM type WHERE parent_id = #{parentId}
    </select>
    
    <select id="findTypeTree" resultMap="TypeResultMap">
        SELECT t.*, p.type_name as parent_name 
        FROM type t
        LEFT JOIN type p ON t.parent_id = p.type_id
        ORDER BY t.sort_order, t.type_id
    </select>
    
    <select id="searchByName" resultMap="TypeResultMap">
        SELECT * FROM type 
        WHERE type_name LIKE CONCAT('%', #{keyword}, '%')
        ORDER BY sort_order, type_id
    </select>

    <insert id="insert" parameterType="com.example.springboot.entity.Type" useGeneratedKeys="true" keyProperty="typeId">
        INSERT INTO type(
            type_name, description, parent_id, sort_order, create_time, update_time, status
        )
        VALUES(
            #{typeName}, #{description}, #{parentId}, #{sortOrder}, NOW(), NOW(), #{status}
        )
    </insert>

    <update id="update" parameterType="com.example.springboot.entity.Type">
        UPDATE type 
        SET type_name = #{typeName},
            description = #{description},
            parent_id = #{parentId},
            sort_order = #{sortOrder},
            update_time = NOW(),
            status = #{status}
        WHERE type_id = #{typeId}
    </update>
    
    <update id="updateStatus">
        UPDATE type
        SET status = #{status},
            update_time = NOW()
        WHERE type_id = #{typeId}
    </update>

    <delete id="delete" parameterType="int">
        DELETE FROM type WHERE type_id = #{typeId}
    </delete>

</mapper> 